#include "mix_par.h"
/*int main()
{
    Mix_Par q;
    q.Printer(6);
    int* teams = new int[8];
    for (int i = 0; i < 8; i++) teams[i] = i;
    ptrdiff_t n = _msize(teams) / sizeof(teams[0]);
    cout << n << endl;
    system("pause");
    return 0;
}
*/
